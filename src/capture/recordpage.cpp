#include "recordpage.h"

RecordPage::RecordPage(QWidget *parent)
    : QWidget(parent),
      camera(nullptr),
      mediaRecorder(nullptr)
{
    // TODO: Initialize camera
    // TODO: Initialize media recorder
    // TODO: Setup recording UI
}
