#include "videoplayer.h"

VideoPlayer::VideoPlayer(QWidget *parent)
    : QWidget(parent),
      mediaPlayer(nullptr),
      videoWidget(nullptr)
{
    // TODO: Initialize QMediaPlayer
    // TODO: Initialize QVideoWidget
    // TODO: Setup UI layout
}
