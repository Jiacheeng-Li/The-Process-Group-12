#include "chatpage.h"

ChatPage::ChatPage(QWidget *parent)
    : QWidget(parent),
      chatListView(nullptr),
      messageInput(nullptr),
      socket(nullptr)
{
    // TODO: Initialize chat UI
    // TODO: Setup socket connection
    // TODO: Setup message handlers
}
